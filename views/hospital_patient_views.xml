<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Patient Form View -->
    <record id="view_hospital_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Patient">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Patient Name"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Personal Information">
                            <group string="Personal Information">
                                <field name="gender"/>
                                <field name="date_of_birth"/>
                                <field name="age"/>
                                <field name="registration_date"/>
                                <field name="active"/>
                                <field name="status"/>
                            </group>
                        </group>
                        <group string="Contact Information">
                            <field name="phone"/>
                            <field name="email"/>
                        </group>
                    </group>

                    <group string="Medical Information">
                        <field name="doctor_id" domain="[('active', '=', True)]" context="{'default_availability_status': 'available'}"/>
                        <field name="doctor_specialty" readonly="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="consultation_fee"/>
                        <field name="disease_ids" widget="many2many_tags" domain="[('active', '=', True)]"/>
                        <field name="notes"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Patient List View -->
    <record id="view_hospital_patient_list" model="ir.ui.view">
        <field name="name">hospital.patient.list</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <list string="Patients">
                <field name="name"/>
                <field name="gender"/>
                <field name="age"/>
                <field name="doctor_id"/>
                <field name="status"/>
                <!-- Necessary fields added to be viewed on list -->
                <!-- <field name="phone"/>
                <field name="email"/>
                -->
            </list>
        </field>
    </record>

    <!-- Patient Window Action -->
    <record id="hospital_patient_action" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first patient record!</p>
        </field>
    </record>
</odoo>